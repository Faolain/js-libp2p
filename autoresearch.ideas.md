- Keep using the browser-to-node WebRTC Direct benchmark (`benchmark/webrtc-browser-direct-perf.mjs`) to validate whether any native-path wins generalize to a real browser path. Current host runs suggest `mtu=1500` helps browser download and latency locally, but upload remains noisy, so this is still not enough evidence to trust it as a generally safe production setting.
- Use the raw `node-datachannel` / libdatachannel microbenchmark (`benchmark/node-datachannel-direct-perf.mjs`) to keep mapping the native stability/performance surface. It already points to a real deeper issue: default MTU is stable, `1200`/`1300` are slower, and the `1400`-`1500` region can crash/abort outright in raw native mode.
- Move from ad-hoc native probing toward a tracked patch path if a promising direction appears: either vendor a minimal node-only direct RTCDataChannel/RTCPeerConnection wrapper for the direct path, or add a clean patch-package-style patch against node-datachannel/libdatachannel so native/path changes can be benchmarked and committed cleanly.
- Revisit `benchmark/webrtc-perf.mjs` timeout-based throughput mode on Node 24 only if the browser benchmark and raw native microbenchmark still leave ambiguity; it is lower priority than validating or replacing the suspicious MTU-driven native win.
