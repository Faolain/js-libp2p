- Keep using the browser-to-node WebRTC Direct benchmark (`benchmark/webrtc-browser-direct-perf.mjs`) to validate whether any native-path wins generalize to a real browser path. Lightweight host sanity runs at `16 MiB` pass for both the current no-mux winner and a mux-restored variant, and the kept `mtu=1500` winner also passes at `20 MiB`, but it still times out by `22 MiB` and above on the host browser path. Nearby MTU compromises at `1450` and `1420` still timed out. Instrumented browser errors on the kept `32 MiB` config report `recentOutputs=[]`, and because the browser harness runs upload first that points specifically to the browser→node upload leg stalling before the first perf sample. A more promising shared-path fix is now to make the kept `mtu=1500` pacing slightly more conservative: lowering `bufferedAmountLowThreshold` from `75%` into roughly the `69%`-`71%` range makes the `32 MiB` browser run pass while keeping the primary ratio close to the frontier (`~0.345`-`0.3479`), whereas `71.5%` and `72%` re-break the browser run. That compromise is still noisy and narrow: `70%` even managed one passing `64 MiB` browser run, but other `64 MiB` runs at `70%` still timed out, and tiny queue retunes around it (`sendBufferSize=3.125 MiB`, `recvBufferSize=2.25 MiB`, `MAX_BUFFERED_AMOUNT=1.75 MiB`) re-broke the browser run. The older browser-viable `mtu=1400 + MAX_BUFFERED_AMOUNT=3 MiB` regime is still useful as a robustness reference — it passes both `32 MiB` and `64 MiB` browser runs and improves the primary ratio to about `0.320`-`0.325`, but it remains clearly slower, and that regime is narrow too (`sendBufferSize=3.25 MiB` or `4 MiB`, `recvBufferSize=1.5 MiB`, or an `80%` wakeup threshold all re-break the `32 MiB` browser run). The next browser step should therefore focus on targeted diagnosis or retuning within the kept-`mtu=1500` / lower-threshold window, while using the slower `mtu=1400` fallback as the browser-robust control.
- Use the raw `node-datachannel` / libdatachannel microbenchmark (`benchmark/node-datachannel-direct-perf.mjs`) to keep mapping the native stability/performance surface. It already points to a real deeper issue: default MTU is stable, `1200`/`1300` are slower, and the `1400`-`1500` region can crash/abort or time out outright in raw native mode, including a fresh current-queue-shape rerun where `1500` segfaulted and `1472` timed out.
- Move from ad-hoc native probing toward a tracked patch path if a promising direction appears. A tracked repo-side direct RTCDataChannel wrapper has now also trailed the current best, so prefer a clean patch-package-style patch against node-datachannel/libdatachannel over more wrapper-layer rewrites.
- If deeper native exploration continues, the two most credible next steps are now: (1) investigate why the `libdatachannel 0.24.x` line only becomes fast and stable in the newer no-mux / small-queue regime (`enableIceUdpMux=false`, `enableIceTcp=false`, `mtu=1500`, `3 MiB` send, `2 MiB` recv, `2 MiB` buffered cap, `75%` resume threshold) while mux restoration still breaks the full benchmark, then decide whether that reflects a real newer-stack integration fault or simply a materially different stability surface, or (2) extend the now-working source-build path beyond default libjuice/OpenSSL by provisioning `libnice-dev` and/or `gnutls` so a real transport-stack swap can be tested instead of just rebuilding the same native stack locally.
- New diagnostics narrowed the 0.24.x fault line further and retired several stale ideas. Gating node-side sends on the actual `open` event and adding a small post-open delay were both worth testing, but neither was sufficient. A minimal two-channel polyfill repro on aggressive `0.32.1` can open the negotiated handshake channel plus one application channel and successfully deliver a single `16 KiB` message, so the newer stack is not simply unable to create one post-handshake channel. But once that same minimal repro starts **repeated** application-channel sends, it fails before delivering any app bytes (about `16 MiB` sent with `mtu=1500`, about `8 MiB` with default/unset MTU) even without libp2p framing, while the paired `0.29.0` repro cleanly transfers `64 MiB`. Follow-up trigger mapping has now ruled out explicit `enableIceTcp` as the sole cause, and has shown something much stronger: disabling **server-side ICE UDP mux** alone makes that smaller repeated-send repro on `0.32.1` transfer the full `64 MiB` successfully under otherwise aggressive settings. That makes server-side UDP mux the clearest trigger candidate in the newer repeated-send regression surface, even though the full libp2p benchmark still needs additional conditions to become stable. The best next path is therefore to focus on what server-side mux changes in the 0.24.x line (candidate type/path, socket ownership, buffering semantics, or close behavior) and/or package the small mux-sensitive repro for upstream node-datachannel/libdatachannel investigation.
- If shared send-path overhead becomes worth revisiting, prefer a correctness-preserving reduction in `bufferedAmount` polling or native/polyfill callback overhead over more threshold micro-tuning; the naive JS-side bufferedAmount estimation experiment already timed out, and fresh near-frontier retunes around the new `3/2/2/75` winner (`sendBufferSize=3 MiB`, `recvBufferSize=2 MiB`, `MAX_BUFFERED_AMOUNT=2 MiB`, `75%`) have not beaten it so far (`recv=2.5 MiB`, `send=3.5 MiB`, `MAX_BUFFERED_AMOUNT=3 MiB`, and `80%` resume all trailed).
- Avoid spending many more primary-loop runs on tiny near-`1500` MTU micro-retunes unless browser or raw-native diagnostics uncover a principled target; `1472` and `1492` already trailed badly while default/unset MTU is unstable under the current best stack.
